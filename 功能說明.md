# 新增功能說明

## 概述
已成功新增三個右鍵選單功能，透過 PowerShell 執行 main.js 來解譯 CS 檔案。

## 新增的功能

### 1. 解譯單一CS檔案
- **右鍵選單名稱**: "解譯單一CS檔案"
- **適用對象**: 所有檔案類型 (*)
- **參數**: `-decodefile`
- **執行命令**: `node "main.js" -f "檔案完整路徑"`
- **使用方式**: 在任何檔案上按右鍵，選擇「解譯單一CS檔案」

### 2. 解譯資料夾內CS檔案
- **右鍵選單名稱**: "解譯資料夾內CS檔案"
- **適用對象**: 資料夾 (Directory)
- **參數**: `-decodefolder`
- **執行命令**: `node "main.js" -s "資料夾路徑"`
- **使用方式**: 在資料夾上按右鍵，選擇「解譯資料夾內CS檔案」
- **說明**: 只處理該資料夾內的 CS 檔案，不包含子資料夾

### 3. 解譯資料夾(含子資料夾)內CS檔案
- **右鍵選單名稱**: "解譯資料夾(含子資料夾)內CS檔案"
- **適用對象**: 資料夾 (Directory)
- **參數**: `-decodesubfolder`
- **執行命令**: `node "main.js" -s "資料夾路徑" -r`
- **使用方式**: 在資料夾上按右鍵，選擇「解譯資料夾(含子資料夾)內CS檔案」
- **說明**: 遞迴處理該資料夾及所有子資料夾內的 CS 檔案

## 技術實作細節

### Registry 鍵值設定
- **單一檔案**: `*\shell\DecodeCsFile`
- **資料夾**: `Directory\shell\DecodeCsFileFolder`
- **資料夾含子資料夾**: `Directory\shell\DecodeCsFileSubFolder`

### 程式執行流程
1. 使用者在檔案總管中對檔案或資料夾按右鍵
2. Windows 透過 Registry 設定呼叫 `RightClickCopyFileName.exe` 並傳入對應參數
3. 程式接收參數後，找到同目錄下的 `main.js`
4. 透過 PowerShell 執行 Node.js 命令來處理檔案
5. 開啟新的 PowerShell 視窗顯示執行過程

### 重要注意事項
1. **main.js 位置**: 必須放在與 `RightClickCopyFileName.exe` 相同的目錄下
2. **Node.js 需求**: 系統必須已安裝 Node.js，且 `node` 命令可在 PowerShell 中執行
3. **首次執行**: 程式需要以管理員權限執行一次（不帶參數）來註冊右鍵選單

## 安裝步驟

1. 以**系統管理員權限**執行 `RightClickCopyFileName.exe`（不帶任何參數）
2. 程式會自動註冊所有右鍵選單項目到 Windows Registry
3. 確認 `main.js` 檔案存在於與 exe 相同的目錄
4. 確認系統已安裝 Node.js

## 使用範例

### 解譯單一檔案
在 `MyClass.cs` 檔案上按右鍵 → 選擇「解譯單一CS檔案」

### 解譯資料夾
在 `MyProject` 資料夾上按右鍵 → 選擇「解譯資料夾內CS檔案」

### 解譯資料夾及子資料夾
在 `MyProject` 資料夾上按右鍵 → 選擇「解譯資料夾(含子資料夾)內CS檔案」

## 程式碼修改摘要

### Program.cs 修改內容
1. 更新了常數定義，修正 Registry 路徑
2. 在 `Main` 方法中新增三個參數處理邏輯：
   - `-decodefile`: 處理單一檔案
   - `-decodefolder`: 處理資料夾
   - `-decodesubfolder`: 處理資料夾及子資料夾
3. 新增三個方法：
   - `解譯單一檔案(string filePath)`
   - `解譯資料夾(string folderPath)`
   - `解譯資料夾及子資料夾(string folderPath)`
4. 更新了三個建立右鍵選單的方法，使用正確的參數和顯示名稱

### main.js
原有的 main.js 已經支援所需的參數：
- `-f`: 單一檔案模式
- `-s`: 資料夾模式
- `-r`: 遞迴模式（與 `-s` 搭配使用）

## 測試建議

1. 重新編譯專案
2. 以管理員權限執行編譯後的 exe 檔案（註冊右鍵選單）
3. 將 `main.js` 複製到與 exe 相同的目錄
4. 在檔案總管中測試三個右鍵選單功能
